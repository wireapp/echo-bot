server:
  applicationConnectors:
    - type: http
      port: 8080

  requestLog:
    appenders: []

auth: ${WIRE_ECHO_SERVICE_TOKEN}   # Service's auth token

module: echo
portMin: 32768
portMax: 32867
ingress: localhost

db:
  host: localhost
  port: 6379
  password: ${REDIS_PASSWORD}